Macros and metaprogramming
==========================

macro_rules

proc_macro
  syn
  proc_macro2
  quote
  proc-macro-error
  -macros crates

hygiene
macros defining macros
``paste``

macro resolution order, defining macros in macros

build.rst
